Headlyne: An RSS Feed 'Viewer'.

lib: Library of nodes used.

List of Nodes
	- urllib.js
		- fetch(url, fn(str)) - callback when URL downloaded (as a string).
			Status: Completed, Unit Tested.
		- parse(rss) - return the RSS Tree for a string.
			Status: Incomplete
		- stripURLs(str) - return an array of URLs from a string.
			Status: Unstarted
